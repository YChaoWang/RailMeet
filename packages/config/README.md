# @railmeet/config

Validated environment configuration for RailMeet services.

Service-specific loaders (`loadApiConfig`, `loadWorkerConfig`, `loadWebConfig`)
parse and validate only the variables each process needs.
