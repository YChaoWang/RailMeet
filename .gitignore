# Dependencies
node_modules/
.pnpm-store/

# Builds / TypeScript emit
dist/
.next/
out/
build/
*.tsbuildinfo

# Source maps and declaration maps outside source (belt-and-suspenders with dist/)
*.js.map
*.d.ts.map

# Synced MapLibre worker assets (regenerated by apps/web `prebuild` / `predev`)
apps/web/public/maplibre/

# Environment (keep the committed example)
.env
.env.*
!.env.example

# Logs and debug
*.log
npm-debug.log*
pnpm-debug.log*
.turbo/
.vitest/

# Test / coverage
coverage/
.nyc_output/
playwright-report/
test-results/
**/blob-report/

# Local platform / CLI state
.vercel/
.railway/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*~

# Secrets / misc
*.pem
.cache/
tmp/
.tmp/
.dump.rdb
dump.rdb

# Catalog production artifacts (download/build locally; too large for git)
packages/catalog/data/cache/
packages/catalog/data/artifacts/
!packages/catalog/data/artifacts/.gitkeep
